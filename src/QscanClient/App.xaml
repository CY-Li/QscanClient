<Application x:Class="QscanClient.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:QscanClient"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="Brush_Background_Base" Color="#121212"/>
        <SolidColorBrush x:Key="Brush_Background_Surface" Color="#1E1E1E"/>
        
        <LinearGradientBrush x:Key="Brush_Accent_Gradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF6B00" Offset="0"/>
            <GradientStop Color="#FF8F00" Offset="1"/>
        </LinearGradientBrush>

        <!-- Standard Theme Brushes (Updated) -->
        <SolidColorBrush x:Key="Brush_Accent" Color="#FF6B00"/>
        <SolidColorBrush x:Key="Brush_Text_Primary" Color="#FFFFFF" Opacity="0.9"/>
        <SolidColorBrush x:Key="Brush_Text_Secondary" Color="#FFFFFF" Opacity="0.6"/>

        <!-- Depth & Shadows -->
        <DropShadowEffect x:Key="StandardShadow" BlurRadius="20" Opacity="0.4" ShadowDepth="4" Color="Black"/>

        <!-- Custom ScrollBar Style -->
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
            <Setter Property="Foreground" Value="#444444" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="10" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="thumbBorder" Background="#333333" CornerRadius="5" Margin="2"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="thumbBorder" Property="Background" Value="#555555" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource Brush_Accent}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>